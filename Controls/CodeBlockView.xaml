<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="networker.Controls.CodeBlockView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:networker.Controls">

    <Border Style="{StaticResource CodeBlockStyle}">
        <StackPanel>
            <Grid Padding="12,8,12,8" ColumnDefinitions="*,Auto,Auto" ColumnSpacing="8">
                <TextBlock
                    x:Name="TitleText"
                    Grid.Column="0"
                    Style="{StaticResource MonoTextStyle}"
                    Foreground="{ThemeResource AppTextSecondaryBrush}"
                    TextTrimming="CharacterEllipsis"/>
                <Border x:Name="BadgeHost" Grid.Column="1" Visibility="Collapsed">
                    <TextBlock x:Name="BadgeText" Style="{StaticResource BadgeTextStyle}"/>
                </Border>
                <Button
                    x:Name="CopyButton"
                    Grid.Column="2"
                    Style="{StaticResource IconButtonStyle}"
                    Click="CopyButton_Click"
                    ToolTipService.ToolTip="Copy to clipboard"
                    AutomationProperties.Name="Copy to clipboard">
                    <FontIcon Glyph="&#xE8C8;" FontSize="14"/>
                </Button>
            </Grid>
            <Border Height="1" Background="{ThemeResource AppBorderBrush}"/>
            <ScrollViewer MaxHeight="480" VerticalScrollBarVisibility="Auto" HorizontalScrollMode="Disabled" Padding="12,10">
                <RichTextBlock x:Name="BodyText" FontFamily="{ThemeResource CodeFontFamily}" FontSize="{ThemeResource FontSizeCode}" TextWrapping="Wrap" IsTextSelectionEnabled="True"/>
            </ScrollViewer>
        </StackPanel>
    </Border>
</UserControl>
