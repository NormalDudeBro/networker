<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="networker.Controls.CodeBlockView"
    x:Name="CodeBlockRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:networker.Controls">

    <Border Style="{StaticResource CodeBlockStyle}">
        <StackPanel>
            <Grid Padding="12,7" ColumnDefinitions="Auto,*,Auto,Auto,Auto" ColumnSpacing="8" Background="{ThemeResource AppInsetBrush}">
                <FontIcon Glyph="&#xE943;" FontSize="12" Foreground="{ThemeResource AppAccentBrush}" VerticalAlignment="Center"/>
                <TextBlock x:Name="TitleText" Grid.Column="1" Style="{StaticResource CodeTitleTextStyle}"/>
                <Border x:Name="BadgeHost" Grid.Column="2" Visibility="Collapsed" VerticalAlignment="Center">
                    <TextBlock x:Name="BadgeText" Style="{StaticResource BadgeTextStyle}"/>
                </Border>
                <TextBlock
                    x:Name="CopyStatus"
                    Grid.Column="3"
                    Text="Copied"
                    Style="{StaticResource InlineSuccessTextStyle}"
                    VerticalAlignment="Center"
                    Visibility="Collapsed"
                    AutomationProperties.LiveSetting="Polite"/>
                <Button
                    x:Name="CopyButton"
                    Grid.Column="4"
                    Style="{StaticResource IconButtonStyle}"
                    Click="CopyButton_Click"
                    ToolTipService.ToolTip="Copy to clipboard"
                    AutomationProperties.Name="Copy to clipboard">
                    <FontIcon x:Name="CopyGlyph" Glyph="&#xE8C8;" FontSize="14"/>
                </Button>
            </Grid>
            <Border Height="1" Background="{ThemeResource AppBorderBrush}"/>
            <ScrollViewer x:Name="CodeScroll" MaxHeight="{StaticResource CodeBlockMaxHeight}" VerticalScrollBarVisibility="Auto" HorizontalScrollMode="Enabled" HorizontalScrollBarVisibility="Auto" Padding="14,12">
                <RichTextBlock x:Name="BodyText" FontFamily="{ThemeResource CodeFontFamily}" FontSize="{ThemeResource FontSizeCode}" TextWrapping="NoWrap" IsTextSelectionEnabled="True"/>
            </ScrollViewer>
        </StackPanel>
    </Border>
</UserControl>
